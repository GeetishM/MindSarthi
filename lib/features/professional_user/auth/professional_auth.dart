import 'package:flutter/material.dart';

class ProfessionalAuth extends StatelessWidget {
  const ProfessionalAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}